/** Shared box for open / home / filters / about SVGs in shell chrome. */
export const navMarkIconImgClassName =
  "pointer-events-none block size-[14px] shrink-0 object-contain select-none";

/** Vertical copy on narrow rails (e.g. Filters, About tab). */
export const navRailVerticalLabelClassName =
  "select-none text-center font-suisseintl text-xs font-normal leading-5 text-text-body [text-orientation:mixed] [writing-mode:vertical-rl] rotate-180";

/** Horizontal chrome link label (e.g. Home). */
export const navChromeLinkLabelClassName =
  "font-suisseintl text-xs font-normal leading-5 text-text-body";
