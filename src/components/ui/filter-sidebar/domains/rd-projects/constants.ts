/**
 * R&D project labels from Strapi when that taxonomy is wired into the filter pipeline.
 * Empty until then — sidebar row stays closed and non-interactive.
 */
export const RD_PROJECT_OPTION_LABELS: readonly string[] = [];
