/**
 * Fellowship filter labels from Strapi when wired into the filter pipeline.
 * Empty until then — sidebar row stays non-interactive (desktop + mobile).
 */
export const FELLOWSHIP_OPTION_LABELS: readonly string[] = [];
